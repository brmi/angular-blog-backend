<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1> Blog</h1>
    <p>username is <%= username %></p>


    <% for(let i = 0; i < length; i++) { %>
        <div class="post">
            <p>postid: <%- posts[i].postid %> </p>
            <a class="title" href='/blog/<%= username %>/<%= posts[i].postid %>'>
                <%- posts[i].title %>  
            </a>

            <a class="body" href='/blog/<%= username %>/<%= posts[i].postid %>'>
                <%- posts[i].body %> 
            </a>

        </div>
    <% } %>
    <% if (nextStartingID > 0) { %>
        <a href='/blog/<%= username %>/?start=<%= nextStartingID %>'> next </a>
    <% } %>

  </body>
</html>
